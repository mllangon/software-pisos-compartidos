{
  "name": "software-pisos-compartidos",
  "private": true,
  "packageManager": "pnpm@9.1.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "dev:api": "pnpm --filter api dev",
    "dev:web": "pnpm --filter web dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "prepare": "husky install || echo 'skip'"
  },
  "devDependencies": {
    "turbo": "^2.1.1",
    "husky": "^9.1.6"
  }
}

